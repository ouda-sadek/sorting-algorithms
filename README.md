# Sorting Algorithms Benchmark
A Python OOP project to compare several sorting algorithms based on their performance (time, CPU, memory).

## Start the project
```bash
pip install -r requirements.txt
python main.py